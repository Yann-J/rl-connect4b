seed: 0
model:
  channels: 64
  blocks: 5
buffer:
  capacity: 4096
selfplay:
  games: 32
  warmup_random_games: 32
  mcts_sims_selfplay: 100
eval:
  mcts_sims_eval: 400
train:
  steps_per_1k_positions: 100
  positions_block: 1000
  batch_size: 64
  lr: 0.01
  cosine_min_lr: 0.0001
output:
  dir: checkpoints/skeleton
logging:
  tensorboard_dir: checkpoints/skeleton/runs

